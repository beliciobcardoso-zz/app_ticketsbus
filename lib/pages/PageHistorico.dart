import 'package:flutter/material.dart';

class HistoricoRecarga extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Histórico de Recarga"),
      ),
    );
  }
}
